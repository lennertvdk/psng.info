# psng.info

A Vite-powered React application using TypeScript, Tailwind CSS, and shadcn-ui components.

## Development

### Requirements

- Node.js 18+ (or compatible)
- npm

### Install dependencies

```sh
npm install
```

### Run locally

```sh
npm run dev
```

### Build for production

```sh
npm run build
```

### Preview production build

```sh
npm run preview
```

### Lint and test

```sh
npm run lint
npm run test
```

## Project Structure

- `src/` – application source code
- `src/components/` – UI and page components
- `src/pages/` – route pages
- `src/lib/` – shared utilities
- `src/hooks/` – custom hooks
- `src/data/` – static data
- `public/` – static assets

## Technologies

- Vite
- React
- TypeScript
- Tailwind CSS
- shadcn-ui
- React Router
- Vitest

## Notes

This repository contains a frontend site and should be run with the included npm scripts. The previous generic Lovable README has been replaced with current project-specific instructions.
